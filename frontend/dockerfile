# frontend/Dockerfile

# -------------------- Stage 1: Build Stage --------------------
FROM node:20 as build-stage

WORKDIR /app

# คัดลอก package.json และ package-lock.json เพื่อติดตั้ง dependencies
COPY package*.json ./
RUN npm install

# คัดลอก source code ทั้งหมด
COPY . .

# สั่ง Build โปรเจกต์ Vite (จะสร้างไฟล์ในโฟลเดอร์ dist)
RUN npm run build

# -------------------- Stage 2: Production/Serve Stage --------------------
FROM nginx:alpine as production-stage

# คัดลอกไฟล์ Build ที่ได้จาก Stage 1 มาไว้ใน Nginx web root
COPY --from=build-stage /app/dist /usr/share/nginx/html

# เปิดพอร์ตสำหรับภายนอก (พอร์ต 80 ใน Container จะถูก Map ไป 3000 ของ Host)
EXPOSE 80

# คำสั่งรัน Nginx (Default)
CMD ["nginx", "-g", "daemon off;"]