/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        // 🌿 ธีม Nature (จากภาพของคุณ)
        primary: '#305949',    // เขียวเข้มสุด (ใช้กับ Navbar, หัวข้อ)
        secondary: '#6A9360',  // เขียวกลาง (ใช้กับปุ่มหลัก)
        accent: '#A8C66C',     // เขียวอ่อน (ใช้กับปุ่มรอง, Hover)
        highlight: '#F0E491',  // เหลืองครีม (ใช้กับป้ายแจ้งเตือน, จุดเด่น)
        
        // สีพื้นหลังใหม่ (แก้ปัญหาจอด้านข้างขาวเกินไป)
        background: '#F2F0E4', // สีครีมเข้ม (พื้นหลังเว็บ)
        card: '#FAFAF5',       // สีการ์ดสินค้า (ขาวเหมือนเดิม เพื่อให้อ่านง่าย)
      }
    },
  },
  plugins: [],
}