server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # บรรทัดนี้คือพระเอกครับ บอกว่าถ้าหาไฟล์ไม่เจอ ให้โยนไป index.html
        try_files $uri $uri/ /index.html;
    }
}